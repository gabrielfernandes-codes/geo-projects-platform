{
  "name": "geo-projects-platform",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22.11.0",
    "pnpm": ">=9.12.0"
  },
  "os": [
    "linux",
    "darwin",
    "current"
  ],
  "cpu": [
    "x64",
    "arm64"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "format": "turbo run format",
    "lint": "turbo run lint",
    "fix": "turbo run fix"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}
