{
  "name": "geo-projects-platform",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22.11.0",
    "pnpm": ">=9.12.0"
  },
  "os": [
    "linux",
    "darwin",
    "current"
  ],
  "cpu": [
    "x64",
    "arm64"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "format": "turbo run format",
    "lint": "turbo run lint",
    "fix": "turbo run fix"
  },
  "dependencies": {
    "dotenv-cli": "^7.4.4"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
